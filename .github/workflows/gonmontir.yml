name: gonmontir
on:
  workflow_dispatch:
  issues:
    types: [opened, edited, deleted, labeled, unlabeled]
jobs:
  New-Action:
    runs-on: ubuntu-latest
    steps:
      - name: Push changes
        run: git push
